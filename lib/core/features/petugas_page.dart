import 'package:flutter/material.dart';

class PetugasPage extends StatelessWidget {
  const PetugasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PETUGAS PAGE')),
    );
  }
}